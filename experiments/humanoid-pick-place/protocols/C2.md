# C2 frozen execution record

Frozen by Codex on 2026-09-14 after Task 007 review, before any C2 model decision.
The user explicitly instructed execution of all five stages through outcome review.

This record freezes the specification in [C2_PROPOSAL.md](C2_PROPOSAL.md), whose
SHA-256 is `a1a3b982fd455c9c2e220787be74c9bbad4c1c0dd6f4e32894aac9a896ced7c0`.
Its historical proposed/unexecuted status and checklist are preserved; this record
supersedes those status fields. The entrypoint still records that exact specification
path/hash, while the pre-run manifest must additionally hash this freeze record.

## Exact condition

- Condition/protocol: `c2` / `humanoid-codex-c2-development`.
- Exactly one randomized supported-G1 episode, development seed 820, fixed pelvis
  and fixed 960 x 720 camera. No walking, recipe, pose-estimator substitution or
  private object/scoring data in policy input. No seeds 840–849.
- Signed-in ChatGPT Codex CLI **0.154.0**, requested model **gpt-5.6-sol**, low reasoning.
  Only Codex chooses VLA actions; AGY operates the unchanged runner and packages data.
  No direct API integration, API keys or Gemini VLA decisions.
- Complete static instruction SHA-256:
  `6d90729760f5458d69dfc6d90e9f433eacd7721f2552e8c24060a840e27237db`.
- Exact serialized geometry SHA-256:
  `d05eaa296612db243fd9362d1018d7b92d63cb93006221f959422fa1712141d6`.
- Existing disabled-tool configuration and JSONL audit; fresh ephemeral decision
  sessions with current public RGB/proprioception/history only.
- 20 decisions maximum; simulated t=25 s including reset; 120 s wall timeout per
  decision. No wrapper retries, fallback controller, rescue action or prompt edits.
- Existing physics, guard, workspace and scorer unchanged. Stop on the first
  rejection, invalid decision, timeout or budget exhaustion and retain that outcome.

The reviewed implementation is `acbf28449049dde6f059b28bfaa8a4c4ab019def`, handed off
at `e336f2054d3ae27ac9f87a38f13d0a9a3cce6b3e`. Execution uses the clean main commit
containing this freeze and Task 008. Record its exact hash and relevant file hashes
**before starting**, then verify those files have not changed after the episode.
Only ignored runtime files may be created while the episode runs.

## Authorized command

From the dedicated Task 008 worktree using the existing interpreter:

```sh
/Users/praveen/work/github/mujoco-llms/.venv/bin/mjpython -m humanoid_sim.codex_policy --execute --condition c2 --output runtime/humanoid/codex-C2/seed-820
```

No live archived-image probe precedes this run; the offline preflight and reviewed
entrypoint tests suffice. If CLI/graphics/source preconditions fail, preserve the
attempt and report the failure. Do not silently start another episode or change
model/version/settings. A failed model episode stays in the denominator of one.

## Outcomes

Primary outcome: existing private scorer's full basket placement, reported 0/1 or
1/1. Report sustained lift, peak object penetration, rejection/errors, final time,
CLI invocation count, decision latencies and available CLI usage. Strict pass
requires placement, <=2 mm peak penetration, t<=25 s and no rejection/execution
failure. Normal budget exhaustion is distinct from execution failure. Do not use
process exit status or `placement_success_claimed=false` as the physical score.

Retain every public request, response, CLI event log, stderr, PNG, static geometry,
private trajectory/score and launch manifest. Hash all archived files. AGY may
compute post-hoc summaries only after the runner exits; it must not feed them back
to the decision sessions. Codex independently audits the returned evidence.

C2 is a descriptive development result on a reused seed, not a statistical C1/C2
comparison or a qualified visual controller. P5 remains separate and unqualified
under its unchanged 5.0 mm mean gate. The exact C2 proposal's interpretation limits
remain in force; do not invent a new success threshold after execution.
