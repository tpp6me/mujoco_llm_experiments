# C3 frozen development execution record

Frozen by Codex on 2026-09-15 after [Task 010 acceptance](../../../coordination/agy/reviews/010-acceptance.md), in response to the user's instruction to continue.

This freezes [C3_PROPOSAL.md](C3_PROPOSAL.md), SHA-256
`caf0febc1493033a03702195e338d9212d411381dac647e06af3acceb417f6c6`.
The proposal remains a historical document; this record supersedes its status.
The entrypoint retains the proposal path/hash. The pre-run manifest additionally
records this document and [C3_FREEZE.json](C3_FREEZE.json).

## Condition and limits

Exactly one supported-G1 randomized development episode, seed 820, fixed pelvis,
fixed 960 x 720 camera. C3 requests the two-field visual assessment and unchanged
command in one signed-in Codex response. Use the exact reviewed instruction, schema,
geometry and source bytes pinned in C3_FREEZE.json. No walking or recipe additions.

Codex CLI 0.154.0, ChatGPT login, requested gpt-5.6-sol, low reasoning. Fresh isolated
tool-disabled sessions; current public RGB/calibration/proprioception/history only.
AGY operates the harness, never substitutes Gemini judgments or robot commands.
No direct OpenAI API calls or API keys. Limit 20 decisions, simulated t=25 s including
reset, 120 seconds per invocation. No probes, retries, rescue, estimator substitution,
extra episode, source edits during execution, or seeds 840–849. Model failure and
normal guard rejection are retained outcomes, not reasons to rerun.

Reviewed code is main `177490e438bcc3a8acc1728ea10cab5814485f1a` (accepted Task010
`9d9e087bd4cf06164435ef9c1761080a490d00a2`). Run from the clean commit containing this
freeze and Task011, recording that exact execution commit before launch. Only ignored
runtime files may be written until the runner exits. Verify the pinned files before
and after the run. Pin model/mesh bytes for this new run, without implying historical
C1/C2 archives contained those per-mesh digests.

## Authorized command

From the dedicated Task011 worktree, once only:

```sh
/Users/praveen/work/github/mujoco-llms/.venv/bin/mjpython -m humanoid_sim.codex_policy --execute --condition c3 --output runtime/humanoid/codex-C3/seed-820
```

Graphics and signed-in CLI execution are authorized for this command. If local
version/login/source/graphics checks fail, preserve the attempted launch and stop.
Do not change configuration or silently start another episode. All failures remain
in the denominator of one requested trial.

## Outcomes and review order

Primary: private scorer full placement, 0/1 or 1/1. Report sustained lift, peak
object penetration, final time, rejections/errors/unknown execution outcomes,
decision count, latency and available CLI usage. Strict pass requires placement,
peak penetration <=2 mm, t<=25 s, and no rejection or execution failure. Process
exit status and placement_success_claimed are not physical-success measurements.

Secondary: retain every image/assessment/command, including uncertain, malformed,
missing and interrupted responses. Codex first labels only saved public images,
without opening model assessments, commands, private trajectory or result summaries.
Use the proposal's exact visibility and finger-relation label definitions. Commit
that first-pass label record before unblinding. Then report field-wise agreement,
uncertainty/abstentions, missing/malformed output and assessment/action consistency
with explicit denominators; compare private trajectory only in a labelled post-hoc
pass. AGY may package inputs and machine-derived accounting but must not supply its
own image labels or interpretation. Its initial handoff must not reveal outcomes
or assessment values before Codex's first-pass labels are saved.

This is one reused development seed, not a statistical comparison, independent
perception benchmark or controller qualification. No inference about internal model
beliefs. Preserve all C1/C2 evidence, strict scoring thresholds and P5's separate
unchanged/unmet 5.0 mm mean gate. No additional experiment follows automatically.
