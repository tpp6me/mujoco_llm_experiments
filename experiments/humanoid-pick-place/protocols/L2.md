# Frozen L2 exact-state LLM development pilot

Frozen: 2026-09-12, before L2 live requests on seeds 700, 701, 702.
This repeats the matched development pilot after fixing L1's API schema error.
It is not a confirmatory benchmark or an independent holdout after L1.

All model, prompt, observation, controller, physics, action/deadline limits,
termination, logging and scoring rules from [L1](L1.md) apply unchanged.
The only request-format change adds explicit `type: string` to the three action
schema discriminators; the API rejected L1's const-only fields before inference.
The requested action capabilities, model settings and prompt are unchanged.
L1's three API failures and the separate diagnostic request remain archived.

L2 uses a fresh output directory and a **$4.50** local estimated/reserved budget.
This leaves room within the original $5 envelope for L1's unknown-usage
reservations and one HTTP-400 diagnostic request. Budget estimates are not invoices.
No model-selected actions or failed episodes are retried within L2.
Freeze the new source and schema before execution. The returned model identity
and all provider attempts must remain in the report. Negative physical outcomes
are valid pilot findings, not reasons to tune within this pilot.
