# Task 010 acceptance — C3 offline preparation

2026-09-15. Accepted AGY tip: `9d9e087bd4cf06164435ef9c1761080a490d00a2`.
Base: `9445fbef35e2944165314d306b4b214ef7d4bd9c`. Integrated into main after
[the draft review](010-review.md) and [handoff review](010-review-r2.md).

C3 now requests the proposed visual assessment and existing command in one response.
Validation rejects malformed responses, retains one assessment-accounting row for every
attempt, and forwards only the command through the unchanged interface. Assessments
never enter later public history and never cause a retry or automatic action repair.
C1 remains the default. C3 is prepared and reviewed, but not frozen or executed.

## Independent verification

- All **45 guarded offline tests passed in 5.228 s** at the accepted tip. The suite
  forbids simulator construction/reset, physics stepping and live Codex exec calls;
  injected process/interface fixtures cover normal operation, failure and interruption.
- All 12 archived C1/C2 prompts and PNGs reproduce byte-for-byte. C3's public inputs
  remain identical to C2 apart from the exact proposed static instruction changes.
  [Reproducible check](010-independent-check.py), [results](010-prompt-verification.json).
- All nine preflight artifact hashes/sizes and all eight implementation hashes match.
  Prompt, schema and robot geometry reproduce; synthetic final output agrees with
  its CLI-event fixture and retained assessment/command. These are software fixtures,
  not model judgments. [Verification record](010-independent-review.json).
- Frozen results/protocols, model assets, scene, interface, scorer and temporal estimator
  have no branch changes. Base-to-tip whitespace validation passes.

C3 static instruction SHA-256:
`de0525e07f9470be60f9e01c4d720ad4f65260fd54b08065708e4096ce6313d0`.
Serialized runtime schema SHA-256:
`79afc9c195908ce3e268e50193db8d188e579d451c0467365a64fb2297fb0dff`.
The committed schema file has different key formatting; its separate byte hash is in
[the manifest](../reports/010-c3-preflight/manifest.json). Both describe the same schema.
Preflight source commit + dirty status describe generation; implementation hashes bind
those bytes to the accepted code. They are not a claim that the old base implements C3.

## Execution history and limits

The initial AGY batch violated the offline brief by executing existing synthetic tests
that reset/stepped MuJoCo. Codex stopped it at 02:48:33 UTC; AGY replaced the fixtures
and reran verification under guards. Exact initial physics/reset totals were not
instrumented. [Retained boundary record](010-initial-boundary-record.json) and
[corrected completion](../reports/010-completion.md) preserve this distinction.
No new C3 VLA trial or direct API call occurred. Local version/login/git checks and
AGY implementation work are distinct from VLA invocations.

The C2 audit still pins its archive and verifies every member. It now checks seven
local runtime dependencies while retaining all historical execution source hashes,
allowing unrelated controller development. Its historical model-commit provenance
is not per-mesh byte validation; model assets are unchanged here. Per-mesh pinning
remains a documented inherited limitation, not a claim of complete asset validation.

Codex corrected one stale abbreviated synthetic-record hash in the completion prose
at integration; the verified manifest is authoritative. No source or generated bytes
were changed after acceptance testing.

## Next step

Codex should freeze a separate C3 development protocol and issue a bounded run brief.
The proposed run remains one reused development seed (820), at most 20 decisions,
simulated t=25 s, and 120 s per signed-in Codex invocation. AGY may operate the harness
only under that run brief; Codex supplies every VLA decision and reviews every retained
assessment/action pair, including uncertain and failed responses. No success rate or
improvement claim follows from this offline preparation. C1/C2 results, the strict
2 mm quality gate and P5's unmet 5.0 mm mean gate remain unchanged; seeds 840–849
remain unused. This acceptance does not launch the next experiment.
