# Task 010 — handoff review R2

Reviewed tip: `109c6c3cd00f9b99b1212c1df63adaf157600c3c`.
Status: focused revisions required. Codex independently passed all 44 guarded tests
(5.152 s), golden preservation of all 12 archived C1/C2 prompts/images, exact C3
instruction/schema, and base-to-tip whitespace validation. Preserve those results.

## R1-A — medium: inferred C3 without metadata fails before invoking the policy

run_visual_episode now infers condition from model_callable.condition when metadata
is omitted. But the C3 call-record model_requested expression evaluates
execution_metadata.get(...) when execution_metadata is None and callable has .model.
Reproduction: FakeSession(), callable with condition='c3', model='gpt-5.6-sol' that
returns a valid assessment/hold, max_calls=1, execution_metadata omitted. Actual:
runner_exception AttributeError 'NoneType' object has no attribute 'get', 0 completed
actions, model_calls=1 though callable was never invoked. Normalize metadata reads
safely and test this route with a fake model counter, plus interrupted/malformed
retention. Do not change C1/C2 defaults or model configuration.

## R4-A — high: completion must disclose the initial boundary violation

The completion's current 'Honest boundary' section falsely claims zero physics/reset
and no unmocked subprocess calls across all work. Initial batch logs prove whole
physics-backed test modules ran repeatedly. The later guarded run does not erase that.
Explicitly record initial batch Environment/reset and synthetic physics-backed tests,
Codex's interruption at 02:48:33 UTC, then subsequent revision checks with guards and
zero physics/reset. No live Codex VLA/API trial was executed. Do not call AGY execution
itself 'zero tokens'. Actual local git and Codex version/login subprocesses did run.
Scope zero-model statements to VLA invocations; distinguish local checks and AGY work.
Keep the original logs referenced at /private/tmp/agy-010-execution/output.jsonl.
Correct PLAN and any other new blanket claims consistently. Replace file:// temporary
worktree links in completion with repo-relative links; plain code paths for runtime logs.

## R5 — medium: bind preflight to final implementation bytes

The preflight/manifest identifies old base 9445fbe + dirty, but includes no script
source hashes. Record final SHA-256 hashes for codex_policy.py, visual_policy_runner.py,
audit_codex_c2.py, run_offline_tests.py, affected tests and committed response schema in
the manifest, with source base/dirty provenance explicitly distinct from handoff tip.
Finish source edits first, generate a NEW preflight directory, verify content/manifest
against final source, then replace only Task010 preflight evidence. Do not silently
recertify old generated bytes. Retain exact generator command and guarded test log.
No real model calls, resets, physics steps, archive rewrite or held-out seeds.

## Audit provenance clarification

The five-file dependency allowlist resolves current controller compatibility. Preserve
historical checks for scoring.py and __init__.py as transitive local imports too.
Do not claim this validates every mesh asset: the historical report has model_commit,
not per-mesh hashes. Record this inherited limitation explicitly instead of asserting
'all simulation/scene assets strictly rejected'. Per-mesh historical pinning is deferred
from Task010; all model assets must remain unchanged in this branch and Codex will
check that diff before acceptance. Preserve existing pinned archive and member checks.

Correct the claim that physical_source_sha256 preserves all 30 historical files if its
actual comprehension filters that mapping. Describe the exact recorded/checking scope.
Finish with ONE guarded affected suite after fixes, fresh hashes/log, commit/push branch,
return exact tip and stop. Do not repeat broad discovery or freeze/run C3.
